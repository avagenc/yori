# For local testing via the dev entry points (cmd/connect, cmd/cli).
# The yori module itself reads none of these.
GEMINI_API_KEY=""
SPOTIFY_CLIENT_ID=""
SPOTIFY_CLIENT_SECRET=""
# Obtain once with `go run ./cmd/connect`, then paste the printed token here.
# The redirect URL (http://localhost:5173/spotify/callback) is fixed in code,
# not here — register it verbatim on the app in the Spotify Developer Dashboard.
DEV_SPOTIFY_REFRESH_TOKEN=""
